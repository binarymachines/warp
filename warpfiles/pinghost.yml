
commands:
        ping_single:
                line: ansible all -i inventory/ -u <user> --private-key <ssh_keyfile> -m ping -l <host>
                defaults:
                        - param: user
                          value: foouser

                        - param: ssh_keyfile
                          value: ~/.ssh/binarymachines_gce_rsa


                

